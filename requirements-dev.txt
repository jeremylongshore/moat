# Moat – Root Development Dependencies
#
# Install with:  pip install -r requirements-dev.txt
# Or via Makefile: make install  (also installs all packages in editable mode)
#
# These are tooling deps only – runtime deps live in each package's
# pyproject.toml under [project.dependencies].

# ── Linting / Formatting ─────────────────────────────────────────────────────
ruff>=0.4.0

# ── Type Checking ─────────────────────────────────────────────────────────────
mypy>=1.9.0
# Pydantic mypy plugin for accurate model type inference
pydantic>=2.0.0

# ── Testing ───────────────────────────────────────────────────────────────────
pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-cov>=5.0.0

# httpx is used by FastAPI's TestClient and for async HTTP in integration tests
httpx>=0.27.0

# ── Security Scanning ─────────────────────────────────────────────────────────
# pip-audit checks installed packages against known CVE databases.
# Run:  pip-audit  (or `make ci` which calls it in CI)
pip-audit>=2.7.0

# ── Optional Dev Utilities ────────────────────────────────────────────────────
# rich makes pytest output and CLI scripts prettier
rich>=13.0.0
